<UserControl x:Class="Exams.UI.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <!--
            <TextBlock Margin="0 0 0 5"
            HorizontalAlignment="Center"
            Style="{StaticResource HeaderTextBlockStyle}"
            Text="Menu" />
        -->
        <Button Margin="5 0 10 10"
                Command="{Binding NavigateStartPage}"
                Content="Home" />
        <Button Margin="5 0 10 10"
                Command="{Binding NavigateCategories}"
                Content="Kategorie" />
        <Button Margin="5 0 10 10"
                Command="{Binding NavigateQuestions}"
                Content="Pytania" />
    </StackPanel>
</UserControl>
